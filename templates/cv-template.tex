\documentclass[11pt, letterpaper]{awesome-cv}

\usepackage{multicol}   % multicolumn list
\usepackage{enumitem}   % list spacing

\setlist{nosep} % list spacing
\geometry{margin=0.5in}
\fontdir[fonts/]
\definecolor{awesome}{HTML}{414141}

% awesome cv data
\name{\V{cv['basics']['name'][0]}}{\V{cv['basics']['name'][1]}}
\address{\V{cv['basics']['location']}}
\mobile{\V{cv['basics']['phone']}}
\email{\V{cv['basics']['email']}}
\position{\V{cv['basics']['titles']|join(', ')}}
\homepage{\V{cv['basics']['website']}}
\github{\V{cv['social_media']['github']['name']}}
\twitter{\V{cv['social_media']['twitter']['name']}}
\makecvfooter{\today}{\V{cv['basics']['name']|join(' ')}}{\thepage}

\begin{document}
\makecvheader

\cvsection{Education}
\begin{cventries}
  \B{for e in cv['education'] -}
  \cventry
  {\V{e['name']}}
  {\V{e['organization']}}
  {\V{e['location']}}
  {\V{e['dates']|join(' - ')}}
  {
  \begin{cvitems} % Description(s) bullet points
  \B{for s in e['summary'] -}
  \item{\V{s}}
  \B{endfor -}
  \end{cvitems}
  }
  \B{endfor -}
\end{cventries}

\cvsection{Work}
\begin{cventries}
  \B{for e in cv['work'] -}
  \cventry
  {\V{e['name']}}
  {\V{e['organization']}}
  {\V{e['location']}}
  {\V{e['dates']|join(' - ')}}
  {
  \begin{cvitems} % Description(s) bullet points
  \B{for s in e['summary'] -}
  \item{\V{s}}
  \B{endfor -}
  \end{cvitems}
  }
  \B{endfor -}
\end{cventries}

\cvsection{Consulting}
\begin{cventries}
  \B{for e in cv['consulting'] -}
  \cventry
  {\V{e['name']}}
  {\V{e['organization']}}
  {\V{e['location']}}
  {\V{e['dates']|join(' - ')}}
  {
  \begin{cvitems} % Description(s) bullet points
  \B{for s in e['summary'] -}
  \item{\V{s}}
  \B{endfor -}
  \end{cvitems}
  }
  \B{endfor -}
\end{cventries}

\cvsection{Volunteer}
\begin{cventries}
  \B{for e in cv['volunteer'] -}
  \cventry
  {\V{e['name']}}
  {\V{e['organization']}}
  {\V{e['location']}}
  {\V{e['dates']|join(' - ')}}
  {}
  \B{endfor -}
\end{cventries}

\cvsection{Publications}
\begin{cventries}
  \B{for e in cv['publications'] -}
  \cventry
  {\V{e['authors']}}
  {\V{e['name']}}
  {\V{e['organization']}}
  {\V{e['date']}}
  {}
  \B{endfor -}
\end{cventries}

\pagebreak

\cvsection{Talks}
\begin{cvhonors}
  \B{for e in cv['talks'] if not e['is_exclude'] -}
  \cvhonor
  {\V{e['name']}}
  {\V{e['organization']}}
  {}
  {\V{e['date']}}
  {}
  \B{endfor -}
\end{cvhonors}

\cvsection{Workshops}
\begin{cventries}
  \B{for e in cv['workshops'] -}
  \cventry
  {\V{e['name']}}
  {\V{e['organization']}}
  {\V{e['location']}}
  {\V{e['date']}}
  {}
  \B{endfor -}
\end{cventries}

\cvsection{Projects}
\begin{cventries}
  \B{for e in cv['projects'] -}
  \cventry
  {\V{e['name']}}
  {\V{e['organization']}}
  {\V{e['location']}}
  {\V{e['dates']|join(' - ')}}
  {}
  \B{endfor -}
\end{cventries}

\cvsection{Awards}
\begin{cvhonors}
  \B{for e in cv['awards'] if not e['is_exclude'] -}
  \cvhonor
  {\V{e['name']}}
  {\V{e['organization']}}
  {}
  {\V{e['date']}}
  {}
  \B{endfor -}
\end{cvhonors}

\cvsection{Certifications}
\begin{description}
  \B{for e in cv['certifications'] -}
  \item[\V{e['name']}] \V{e['organization']}
  \B{endfor -}
\end{description}

\end{document}
