\documentclass[11pt, letterpaper]{awesome-cv}

\usepackage{multicol}   % multicolumn list
\usepackage{enumitem}   % list spacing

\setlist{nosep} % list spacing
\geometry{margin=0.5in}
\fontdir[fonts/]
\definecolor{awesome}{HTML}{414141}

% awesome cv data
\name{\V{cv['basics']['name'][0]}}{\V{cv['basics']['name'][1]}}
\address{\V{cv['basics']['location']}}
\mobile{\V{cv['basics']['phone']}}
\email{\V{cv['basics']['email']}}
\homepage{\V{cv['basics']['website']}}
\position{\V{cv['basics']['titles']|join(', ')}}

\makecvfooter{\today}{\V{cv['basics']['name']|join(' ')}}{\thepage}

\begin{document}
\makecvheader

\cvsection{Education}
\begin{cventries}
  \B{for e in cv['education'] -}
  \cventry
  {\V{e['name']}}
  {\V{e['organization']}}
  {\V{e['location']}}
  {\V{e['dates']|join(' - ')}}
  {}
  \B{endfor -}
\end{cventries}

\cvsection{Work}
\begin{cventries}
  \B{for e in cv['work'] -}
  \cventry
  {\V{e['name']}}
  {\V{e['organization']}}
  {\V{e['location']}}
  {\V{e['dates']|join(' - ')}}
  {
  \begin{cvitems} % Description(s) bullet points
  \B{for s in e['summary'] -}
  \item{\V{s}}
  \B{endfor -}
  \end{cvitems}
  }
  \B{endfor -}
\end{cventries}

\cvsection{Volunteer}
\begin{cventries}
  \B{for e in cv['volunteer'] -}
  \cventry
  {\V{e['name']}}
  {\V{e['organization']}}
  {\V{e['location']}}
  {\V{e['dates']|join(' - ')}}
  {}
  \B{endfor -}
\end{cventries}

\cvsection{Talks}
\begin{cventries}
  \B{for e in cv['talks'] -}
  \cventry
  {\V{e['name']}}
  {\V{e['organization']}}
  {\V{e['location']}}
  {\V{e['date']}}
  {}
  \B{endfor -}
\end{cventries}

\cvsection{Workshops}
\begin{cventries}
  \B{for e in cv['workshops'] -}
  \cventry
  {\V{e['name']}}
  {\V{e['organization']}}
  {\V{e['location']}}
  {\V{e['date']}}
  {}
  \B{endfor -}
\end{cventries}

\cvsection{Projects}
\begin{cventries}
  \B{for e in cv['projects'] -}
  \cventry
  {\V{e['name']}}
  {\V{e['organization']}}
  {\V{e['location']}}
  {\V{e['dates']|join(' - ')}}
  {}
  \B{endfor -}
\end{cventries}

\cvsection{Awards}
\begin{cvhonors}
  \B{for e in cv['awards'] -}
  \cvhonor
  {\V{e['name']}}
  {\V{e['organization']}}
  {}
  {\V{e['date']}}
  {}
  \B{endfor -}
\end{cvhonors}

\cvsection{Skills}
\B{set size = cv['skills']|length -}
\begin{multicols}{\V{size}}
  \begin{itemize}
    \B{for e in cv['skills'] -}
    \B{for s in e['keywords'] -}
    \item {\V{s}}
    \B{endfor -}
    \B{endfor -}
  \end{itemize}
\end{multicols}

\cvsection{Certifications}
\begin{description}
  \B{for e in cv['certifications'] -}
  \item[\V{e['name']}] \V{e['organization']}
  \B{endfor -}
\end{description}

\cvsection{Languages}
\begin{description}
  \B{for e in cv['languages'] -}
  \item[\V{e['name']}] \V{e['fluency']}
  \B{endfor -}
\end{description}
\end{document}
